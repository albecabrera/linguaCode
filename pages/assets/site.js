const dialog=document.querySelector('#share');const urlInput=document.querySelector('#url');
const filterButtons=document.querySelectorAll('[data-filter]');const exerciseCards=document.querySelectorAll('.grid article');const filterEmpty=document.querySelector('#filter-empty');
filterButtons.forEach(button=>button.addEventListener('click',()=>{const filter=(button.dataset.filter||'all').trim().toLocaleLowerCase('de-DE');let visible=0;filterButtons.forEach(item=>{const active=item===button;item.classList.toggle('active',active);item.setAttribute('aria-pressed',active?'true':'false')});exerciseCards.forEach(card=>{const subject=(card.dataset.subject||'').trim().toLocaleLowerCase('de-DE');const matches=filter==='all'||subject===filter;card.hidden=!matches;if(matches)visible++});filterEmpty.hidden=visible>0}));
document.querySelectorAll('[data-share]').forEach(button=>button.addEventListener('click',()=>{const url=new URL(button.dataset.share,location.href).href;urlInput.value=url;const code=qrcode(0,'M');code.addData(url);code.make();document.querySelector('#qr').src=code.createDataURL(6,0);dialog.showModal();}));
document.querySelector('.close').addEventListener('click',()=>dialog.close());
document.querySelector('#copy').addEventListener('click',async()=>{await navigator.clipboard.writeText(urlInput.value);document.querySelector('#copy').textContent='Kopiert';});
